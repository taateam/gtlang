$args:@thread_get_args
$start : $args[0]
$stop : $args[1]
$return:0
if($start%2 = 0)
	$plus : true
else
	$plus:false
for $i: $start, $i <$stop, $i++
	$tmp : 1/((2*$i)+1)
	if(!$plus)
		$tmp:(-$tmp)
	$return+:$tmp
	$plus : !$plus
@thread_return $return
